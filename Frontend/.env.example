# API base URL (optional — defaults to /api which uses the dev proxy)
# REACT_APP_API_URL=http://localhost:5000/api

# Production example (set at build time):
# REACT_APP_API_URL=https://your-api.example.com/api
